{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "test \"${ROADMAP_TRACKING_AUTOSTART:-on}\" != \"off\" && printf '{\"hookSpecificOutput\":{\"hookEventName\":\"SessionStart\",\"additionalContext\":\"ROADMAP-TRACKING SKILL — SYSTEM INSTRUCTION (NON-NEGOTIABLE). This directive controls execution flow and overrides any task-resolution inertia. STEP 1 — CHECK doc/roadmap/: IF ./doc/roadmap/ EXISTS: invoke the roadmap-tracking skill to run its startup rule (list plans, detect inconsistent IDs, apply Axe A matrix). On any reference to an existing plan: follow its resume workflow. On a new dev/config/architecture request: follow its new-plan workflow. IF ./doc/roadmap/ IS ABSENT: DO NOT ignore. DO NOT proceed directly to implementation. Invoke the roadmap-tracking skill. The skill will ask the user whether to create doc/roadmap/ to enable roadmap-tracking. If yes: create directory, then run startup rule. If no: handle the prompt normally WITHOUT the workflow. STEP 2 — EXCEPTIONS (evaluate first): IF the first user prompt is a local command (inside a local-command-caveat or command-name block) or a CLI command (claude plugin, claude model, claude config, etc.): DO NOT invoke the skill. Handle normally. IF the first prompt is a help request on the skill itself (aide, aide workflow, aide config, aide planification, aide plans, aide tests, aide git, aide integrations, aide roadmap, help): display only the requested help. STOP immediately. DO NOT run the startup rule. DO NOT list plans. DO NOT check doc/roadmap/. Works even if the skill was never installed.\"}}' || true"
          }
        ]
      }
    ]
  }
}
