{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "test -d ./doc/roadmap && test \"${ROADMAP_TRACKING_AUTOSTART:-on}\" != \"off\" && printf '{\"hookSpecificOutput\":{\"hookEventName\":\"SessionStart\",\"additionalContext\":\"This project has a doc/roadmap/ directory. Before handling the first prompt, invoke the roadmap-tracking skill to run its startup rule (list plans, detect inconsistent IDs, handle absent doc/roadmap/ case). On any reference to an existing plan, follow its resume workflow. On a new dev/config/architecture request, follow its new-plan workflow. EXCEPTION: If the first user prompt is a help request on the skill itself (aide, aide workflow, aide config, aide planification, aide plans, aide tests, aide git, aide integrations, aide roadmap, help), display only the requested help and STOP — do not run the startup rule, do not list plans, do not check doc/roadmap/. This exception works even on projects that have never had the skill.\"}}' || true"
          }
        ]
      }
    ]
  }
}
